import React, { FC } from "react";

export const Page: FC<Record<string, never>> = () => {
    return (
        <div>
            <div>Hello world~!</div>
        </div>
    );
};

export default Page;
